{
  "name": "paseo-defer",
  "private": true,
  "version": "1.1.1",
  "license": "MIT",
  "description": "Defer messages to Paseo coding agents until a chosen time or usage-window reset.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tomgrin10/paseo-defer.git"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "check": "node check-bundles.mjs && node check-format.mjs && node check-engine.mjs && node check-composer.mjs && node check-pill.mjs && node check-gitinstall.mjs && node check-teardown.mjs",
    "verify": "npm run typecheck && npm run check"
  },
  "devDependencies": {
    "@babel/parser": "^8.0.4",
    "@getpaseo/client": "^0.7.0",
    "@getpaseo/protocol": "^0.7.0",
    "@tanstack/react-query": "^5.90.11",
    "@types/react": "~19.2.0",
    "esbuild": "^0.28.2",
    "react": "19.1.0",
    "react-native": "0.81.5",
    "typescript": "^5.9.3",
    "zod": "^4.4.3"
  }
}
